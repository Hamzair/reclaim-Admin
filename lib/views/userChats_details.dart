import 'package:flutter/material.dart';

class UserchatsDetails extends StatelessWidget {
  const UserchatsDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
